version: 1

project:
  name: "Literature Synthesis"
  output_dir: "reports"

data:
  datasets: []
  subjects: []

analyses: []

generation:
  tasks:
    - id: "from_literature"
      type: "from_literature"
      path: "data/raw/LiteratureOverview.xlsx"
      sheet: "DataSets"
      n_samples: 10000
      output_dir: "data/bundles"
